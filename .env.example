# Copy to .env — defaults match docker-compose.yml, no secrets needed.
NODE_ENV=development
PORT=53001
DATABASE_URL=postgresql://bets:bets@localhost:55435/bets
KAFKA_BROKERS=localhost:59092
OUTBOX_POLL_MS=500
